package xitrum.comet

import xitrum.Action
/*
class Msgs(size: Int) {
  private val msgs = new Array[(Long, Any)]()
  private var head = 0
  private var tail = 0


  def add(msg: Any) {

  }
}

// TODO: support cluster using Hazelcast
object Comet {
  private val channels_msgs = new MMap[String, Array[(Long, Any)]]

  def getMsg(channel: String, timestamp: Long): Any {

  }
}

@GET("/xitrum/comet/:channel/:timestamp")
class CometAction extends Action {
  override def execute {
    val channel   = param("channel")
    val timestamp = tparam[Long]("timestamp")
  }
}
*/
