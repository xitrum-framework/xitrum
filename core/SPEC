== App

trait App {
  def call(remoteIp: String,
           channel:  Channel,
           request:  HttpRequest,
           response: HttpResponse,
           env:      scala.collection.mutable.Map[String, Any])
}

== env

path_info
ignore_response

uriParams
-> params
